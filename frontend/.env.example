# Browser calls (relative /api is OK — Next.js rewrites to the gateway)
NEXT_PUBLIC_API_URL=/api
# Absolute gateway URL used by Next.js server routes (login) and rewrites
DOCS_API_PROXY_TARGET=http://localhost:5100
